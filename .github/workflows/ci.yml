name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      # Match publish.yml: the lockfile is npm 11, node 22 ships npm 10 which
      # misreads it ("missing from lock file").
      - run: npm install -g npm@11.5.1

      - run: npm ci

      - run: npm run verify
